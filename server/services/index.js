'use strict';

const config = require( './config' );
const permalinks = require( './permalinks' );
const validation = require( './validation' );

module.exports = {
  config,
  permalinks,
  validation,
};
